version: '3.4'

services:
    mongo:
        image: mongo:4
        networks:
            backend:
        restart: always

    mongoexpress:
        image: mongo-express
        ports:
            - "8081:8081"
        depends_on:
          - mongo
        restart: always
        networks:
            backend:

networks:
    backend:
